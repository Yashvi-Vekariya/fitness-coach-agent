{
  "name": "fitness-coach-agent",
  "version": "1.0.0",
  "description": "AI Fitness Coach Agent powered by OpenClaw - Web App + Telegram Bot",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../telegram-bot && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:telegram": "cd telegram-bot && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:telegram\"",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "start:backend": "cd backend && npm start",
    "start:telegram": "cd telegram-bot && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
